/**
 * ertmp.h — Enhanced RTMP extension types (public API)
 *
 * This is the public header for Enhanced RTMP v1/v2 support.
 * It includes the internal ertmp header which provides all type
 * definitions and function declarations.
 */
#ifndef LRTMP2_ERTMP_H
#define LRTMP2_ERTMP_H

/* Include the full internal header.
 * Tests and internal code use #include "ertmp/ertmp.h" directly.
 * External consumers use #include <librtmp2/ertmp.h>. */
#include "ertmp/ertmp.h"

#endif
